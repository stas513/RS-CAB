'use client';

// components
import Loader from '@/app/(RSWeb)/common/loader/Loader';

// ----------------------------------------------------------------------

export default function Loading() {
  return <Loader />;
}
