import { BookingCreateView } from '../components/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Create a new user',
};

export default function BookingCreatePage() {
  return <BookingCreateView />;
}
